AddReviewAction:
  properties:
    approved:
      description: Whether the review gives approval or rejects/requests changes
      title: Approved
      type: boolean
    comment:
      title: Comment
      type: string
    when:
      allOf:
      - $ref: 'When'
      default:
        repo:
        - '*'
      title: When
  required:
  - approved
  title: AddReviewAction
  type: object
